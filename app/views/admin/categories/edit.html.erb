<!--=== 修改分类 ===-->
<h2><%= t('category-edit') %></h2>

<!--=== 修改分类表单 ===-->
<%= simple_form_for [:admin, @category] do |f| %>

<div class="group">
  <%= f.select :category_group_id, @category_groups %>
  <%= f.input :name, :label => t('f-category-name') %>
</div>

<%= f.submit t('f-submit'), class: 'btn btn-lg btn-default' %>

<% end %>
